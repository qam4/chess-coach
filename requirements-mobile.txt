# Minimal dependencies for mobile (Termux/Android).
#
# Avoids pydantic v2 (needs Rust to compile pydantic-core on aarch64).
# Uses pydantic v1 which is pure Python and works everywhere.
# FastAPI 0.99.x is the last version that supports pydantic v1.

python-chess>=1.10
httpx>=0.27
pyyaml>=6.0
click>=8.1
fastapi==0.99.1
pydantic>=1.10,<2.0
uvicorn>=0.29
rich>=13.0
